<UserControl x:Class="Fenix.PropertiesGridManager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Fenix"
             mc:Ignorable="d"
             d:DesignHeight="446.667" d:DesignWidth="366.667">
    <UserControl.Resources>
        <CollectionViewSource x:Key="GroupedProperties" Source="{Binding Rows}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Category" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>

        <Style TargetType="GroupItem">
            <Setter Property="Margin" Value="0,0,0,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupItem">
                        <Border BorderBrush="{DynamicResource Th.BorderBrush}" BorderThickness="1" CornerRadius="4" Background="{DynamicResource Th.SurfaceBrush}">
                            <Expander IsExpanded="True" Background="Transparent">
                                <Expander.Header>
                                    <Border Padding="8,5" Background="{DynamicResource Th.Surface2Brush}" BorderBrush="{DynamicResource Th.BorderBrush}" BorderThickness="0,0,0,1">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{DynamicResource Th.TextBrush}" />
                                    </Border>
                                </Expander.Header>
                                <ItemsPresenter />
                            </Expander>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Height" Value="32" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="BorderBrush" Value="{DynamicResource Th.BorderBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Th.HoverBrush}" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Th.SelBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{DynamicResource Th.Surface2Brush}" />
            <Setter Property="Foreground" Value="{DynamicResource Th.TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Th.BorderBrush}" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="10,8"
                Margin="0,0,0,8"
                Background="{DynamicResource Th.SurfaceBrush}"
                BorderBrush="{DynamicResource Th.BorderBrush}"
                BorderThickness="1"
                CornerRadius="4">
            <TextBlock Text="{Binding SelectedObjectName}" FontWeight="SemiBold" />
        </Border>

        <ListView Grid.Row="1"
                  x:Name="PropertyList"
                  ItemsSource="{Binding Source={StaticResource GroupedProperties}}"
                  BorderThickness="1"
                  BorderBrush="{DynamicResource Th.BorderBrush}"
                  Background="{DynamicResource Th.SurfaceBrush}"
                  IsEnabled="{Binding IsEditorEnabled}"
                  Loaded="PropertyList_Loaded"
                  SizeChanged="PropertyList_SizeChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="170" Header="Property" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Width="200" Header="Value">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Grid Margin="0,1" ToolTip="{Binding Description}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <local:ClearableTextBox Grid.Column="0"
                                                            Text="{Binding ValueText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                            IsEnabled="{Binding IsEditable}">
                                        <local:ClearableTextBox.Style>
                                            <Style TargetType="local:ClearableTextBox">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsClearableTextEditorVisible}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </local:ClearableTextBox.Style>
                                    </local:ClearableTextBox>

                                    <TextBox Grid.Column="0"
                                             Text="{Binding ValueText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                             IsReadOnly="{Binding IsReadOnly}">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Setter Property="Margin"     Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsTextEditorVisible}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>

                                    <CheckBox Grid.Column="0"
                                              IsChecked="{Binding BoolValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              IsEnabled="{Binding IsEditable}"
                                              VerticalAlignment="Center"
                                              Margin="4,0,0,0">
                                        <CheckBox.Style>
                                            <Style TargetType="CheckBox">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsBoolEditorVisible}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </CheckBox.Style>
                                    </CheckBox>

                                    <ComboBox Grid.Column="0"
                                              ItemsSource="{Binding EnumValues}"
                                              SelectedItem="{Binding EnumValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              IsEnabled="{Binding IsEditable}">
                                        <ComboBox.Style>
                                            <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Setter Property="Margin"     Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEnumEditorVisible}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ComboBox.Style>
                                    </ComboBox>

                                    <ComboBox Grid.Column="0"
                                              ItemsSource="{Binding StandardValues}"
                                              SelectedItem="{Binding StandardValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              IsEnabled="{Binding IsEditable}">
                                        <ComboBox.Style>
                                            <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Setter Property="Margin"     Value="0" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsStandardValuesEditorVisible}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ComboBox.Style>
                                    </ComboBox>

                                    <Border Grid.Column="0"
                                            Grid.ColumnSpan="2"
                                            Margin="0,1"
                                            BorderBrush="{DynamicResource Th.BorderBrush}"
                                            BorderThickness="1"
                                            Background="{Binding ColorPreviewBrush}"
                                            MouseDown="ColorPreview_MouseDown"
                                            Cursor="Hand">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsColorPickerVisible}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding ColorHexText}"
                                                   Padding="4,0"
                                                   VerticalAlignment="Center"
                                                   Foreground="{Binding ColorForegroundBrush}" />
                                    </Border>

                                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="4,0,0,0">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCollectionEditorVisible}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <TextBlock Text="{Binding CollectionSummary}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>