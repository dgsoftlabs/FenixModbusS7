<Window x:Class="Fenix.AddScriptFromTemplate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fenix"
        mc:Ignorable="d"
        Title="Add Script From Template" Height="380" Width="480" WindowStartupLocation="CenterOwner"
        Background="{StaticResource Th.SurfaceBrush}">
    <Grid Style="{StaticResource Th.DialogRootGrid}" Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Image Style="{StaticResource Th.DialogHeaderIcon}" Source="pack://application:,,,/TreeImage/CsFile.ico" />
            <TextBlock Grid.Row="1" Text="Select a template:" Style="{StaticResource Th.DialogSectionTitle}" />
            <ListBox Grid.Row="2" x:Name="LbTemplates" Style="{StaticResource Th.DialogList}" SelectionChanged="LbTemplates_SelectionChanged" />
        </Grid>

        <StackPanel Grid.Row="1">
            <TextBlock Text="New script name:" Style="{StaticResource Th.DialogSectionTitle}" />
            <TextBox x:Name="TbName" />
        </StackPanel>

        <StackPanel Grid.Row="2" Style="{StaticResource Th.DialogBottomActionsPanel}">
            <Button Style="{StaticResource Th.DialogBottomPrimaryActionButton}" Content="OK" Click="Button_OK_Click" />
            <Button Style="{StaticResource Th.DialogBottomActionButton}" Content="Cancel" Click="Button_Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
