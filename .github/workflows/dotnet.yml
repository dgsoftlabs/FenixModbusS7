name: Build and Release

on:
  push:
  pull_request:

jobs:
  build:
    name: Build and package
    runs-on: windows-2022

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Setup Advanced Installer
        uses: caphyon/advinst-github-action@main
        with:
          advinst-version: '23.9'
          advinst-enable-automation: 'true'
          # advinst-license: ${{ secrets.ADVINST_LICENSE_KEY }}

      - name: Restore NuGet packages
        run: dotnet restore Fenix.sln

      - name: Run tests
        run: dotnet test ProjectDataLib.Test\ProjectDataLib.Test.csproj -c Release --verbosity normal

      - name: Publish Fenix
        run: dotnet publish Fenix\Fenix.csproj -c Release /p:PublishProfile=FolderProfile

      - name: Publish FenixServer
        run: dotnet publish FenixServer\FenixServer.csproj -c Release /p:PublishProfile=FolderProfile

      - name: Publish FenixServer.Api
        run: dotnet publish FenixServer.Api\FenixServer.Api.csproj -c Release /p:PublishProfile=FolderProfile

      - name: Build installer
        uses: caphyon/advinst-github-action@main
        with:
          advinst-version: '23.9'
          advinst-enable-automation: 'true'
          # advinst-license: ${{ secrets.ADVINST_LICENSE_KEY }}
          aip-path: ${{ github.workspace }}\FenixSetup\FenixSetup.aip
          aip-build-name: DefaultBuild
          aip-package-name: FenixSetup.msi
          aip-output-dir: ${{ github.workspace }}\FenixSetup\FenixSetup-SetupFiles

        # Only when to Master

      - name: Read version
        id: version
        shell: pwsh
        run: |
          [xml]$xml = Get-Content version.xml
          echo "value=$($xml.Fenix.version)" >> $env:GITHUB_OUTPUT

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: FenixSetup-v${{ steps.version.outputs.value }}
          path: FenixSetup\FenixSetup-SetupFiles\*.msi
          retention-days: 1

      - name: Create GitHub Release
        if: github.ref == 'refs/heads/master' && github.event_name == 'push'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.value }}
          name: Release v${{ steps.version.outputs.value }}
          files: FenixSetup\FenixSetup-SetupFiles\*.msi
          token: ${{ secrets.GITHUB_TOKEN }}
