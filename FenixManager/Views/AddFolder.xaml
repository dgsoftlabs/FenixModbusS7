<Window  x:Class="FenixWPF.AddFolder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FenixWPF"
        mc:Ignorable="d"
        Title="Add Folder" Height="260" Width="430" WindowStartupLocation="CenterScreen">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Image Width="26" Height="26" Margin="0,0,0,10" Source="pack://application:,,,/TreeImage/Folder.ico" HorizontalAlignment="Left" />
            <RadioButton x:Name="Ch1" FontWeight="SemiBold" IsChecked="True" Content="Add New Folder" />
            <TextBox x:Name="TbNewFile" IsEnabled="{Binding ElementName=Ch1, Path=IsChecked}" />

            <RadioButton FontWeight="SemiBold" x:Name="Ch2" Content="Add Existing Folder Content" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox x:Name="TbAddFile" IsEnabled="{Binding IsChecked, ElementName=Ch2}" IsReadOnly="True" Margin="0,0,6,8" />
                <Button IsEnabled="{Binding IsChecked, ElementName=Ch2}" Content="Browse" Click="Button_File_Click" Grid.Column="1" MinWidth="80" />
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button MinWidth="80" Margin="0,0,6,0" Content="OK" Click="Button_OK_Click" />
            <Button MinWidth="80" Content="Cancel"  Click="Button_Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>