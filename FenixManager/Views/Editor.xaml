<UserControl x:Class="FenixWPF.Editor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
	         xmlns:forms="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Click="saveFileClick" ToolTip="Save">
                <TextBlock Text="💾" FontSize="14" VerticalAlignment="Center" />
            </Button>
            <Button Command="Cut" ToolTip="Cut">
                <TextBlock Text="✂️" FontSize="14" VerticalAlignment="Center" />
            </Button>
            <Button Command="Copy" ToolTip="Copy">
                <TextBlock Text="📋" FontSize="14" VerticalAlignment="Center" />
            </Button>
            <Button Command="Paste" ToolTip="Paste">
                <TextBlock Text="📌" FontSize="14" VerticalAlignment="Center" />
            </Button>
            <Button Command="Delete" ToolTip="Delete">
                <TextBlock Text="🗑️" FontSize="14" VerticalAlignment="Center" />
            </Button>
            <Separator />
            <Button Click="Button_Click" ToolTip="Search">
                <TextBlock Text="🔍" FontSize="14" VerticalAlignment="Center" />
            </Button>
            <Button Command="Undo" ToolTip="Undo">
                <TextBlock Text="↩️" FontSize="14" VerticalAlignment="Center" />
            </Button>
            <Button Command="Redo" ToolTip="Redo">
                <TextBlock Text="↪️" FontSize="14" VerticalAlignment="Center" />
            </Button>
            <Separator />
            <CheckBox IsChecked="{Binding ElementName=textEditor,Path=WordWrap}" ToolTip="Word Wrap">
                <TextBlock Text="↵" FontSize="14" VerticalAlignment="Center" />
            </CheckBox>
            <CheckBox IsChecked="{Binding ElementName=textEditor,Path=ShowLineNumbers}">
                <TextBlock Width="16" TextAlignment="Center">#</TextBlock>
            </CheckBox>
            <CheckBox IsChecked="{Binding ElementName=textEditor,Path=Options.ShowEndOfLine}">
                <TextBlock Width="16" TextAlignment="Center">�</TextBlock>
            </CheckBox>
            <ComboBox Name="highlightingComboBox"
				SelectedItem="{Binding SyntaxHighlighting, ElementName=textEditor}"
				ItemsSource="{Binding Source={x:Static avalonEdit:HighlightingManager.Instance}, Path=HighlightingDefinitions}"
				SelectionChanged="HighlightingComboBox_SelectionChanged" />
            <Slider Width="100" Minimum="8" Maximum="32" TickFrequency="1" IsSnapToTickEnabled="True"
                    Value="{Binding ElementName=textEditor,Path=FontSize, Mode=TwoWay}" />
        </ToolBar>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <avalonEdit:TextEditor
				Name="textEditor"
				FontFamily="Consolas"
				FontSize="10pt"
				SyntaxHighlighting="C#">
            </avalonEdit:TextEditor>

            <Grid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="CopyHtmlToClipboard" Click="MenuItem_Click">
                    </MenuItem>
                </ContextMenu>
            </Grid.ContextMenu>
        </Grid>
    </DockPanel>
</UserControl>