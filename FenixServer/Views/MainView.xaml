<UserControl x:Class="FenixServer.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="190" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="White" BorderBrush="#D9DEE7" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="16" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Width="135" Command="{Binding OpenProjectCommand}">
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="&#xE838;" FontFamily="Segoe MDL2 Assets" Margin="0,0,6,0" VerticalAlignment="Center" />
            <TextBlock Text="Open Project" VerticalAlignment="Center" />
          </StackPanel>
        </Button>
        <Button Grid.Column="1" Width="100" Command="{Binding StartCommand}" Style="{StaticResource ButtonPrimary}">
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="&#xE768;" FontFamily="Segoe MDL2 Assets" Margin="0,0,6,0" VerticalAlignment="Center" />
            <TextBlock Text="Start" VerticalAlignment="Center" />
          </StackPanel>
        </Button>
        <Button Grid.Column="2" Width="100" Command="{Binding StopCommand}">
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="&#xE71A;" FontFamily="Segoe MDL2 Assets" Margin="0,0,6,0" VerticalAlignment="Center" />
            <TextBlock Text="Stop" VerticalAlignment="Center" />
          </StackPanel>
        </Button>
        <Button Grid.Column="3" Width="110" Command="{Binding OpenInBrowserCommand}">
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="&#xE774;" FontFamily="Segoe MDL2 Assets" Margin="0,0,6,0" VerticalAlignment="Center" />
            <TextBlock Text="Browser" VerticalAlignment="Center" />
          </StackPanel>
        </Button>

        <StackPanel Grid.Column="5" Orientation="Horizontal" VerticalAlignment="Center">
          <TextBlock Text="Project:" FontWeight="SemiBold" Margin="0,0,6,0" />
          <TextBlock Text="{Binding CurrentProjectPath}" TextTrimming="CharacterEllipsis" />
        </StackPanel>
      </Grid>
    </Border>

    <Grid Grid.Row="1" Margin="0,0,0,10">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="2*" />
        <ColumnDefinition Width="10" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <GroupBox Grid.Column="0" Header="Connections">
        <DataGrid ItemsSource="{Binding Connections}"
                  SelectedItem="{Binding SelectedConnection, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Margin="0">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
            <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="*" />
            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
          </DataGrid.Columns>
        </DataGrid>
      </GroupBox>

      <GroupBox Grid.Column="2" Header="Selected Connection Properties">
        <DataGrid ItemsSource="{Binding Properties}"
                  SelectedItem="{Binding SelectedProperty, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Margin="0">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Property" Binding="{Binding Name}" Width="2*" />
            <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="3*" />
          </DataGrid.Columns>
        </DataGrid>
      </GroupBox>
    </Grid>

    <GroupBox Grid.Row="2" Header="Events" Margin="0,0,0,10">
      <DockPanel>
        <Button DockPanel.Dock="Top" Width="100" HorizontalAlignment="Left" Margin="0,0,0,8" Command="{Binding ClearEventsCommand}">
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" Margin="0,0,6,0" VerticalAlignment="Center" />
            <TextBlock Text="Clear" VerticalAlignment="Center" />
          </StackPanel>
        </Button>
        <ListView ItemsSource="{Binding Events}">
          <ListView.View>
            <GridView>
              <GridViewColumn Header="Date" Width="170" DisplayMemberBinding="{Binding frDateTime}" />
              <GridViewColumn Header="Message" Width="900" DisplayMemberBinding="{Binding Mess}" />
            </GridView>
          </ListView.View>
        </ListView>
      </DockPanel>
    </GroupBox>

    <Border Grid.Row="3" Background="White" BorderBrush="#D9DEE7" BorderThickness="1" CornerRadius="4" Padding="10">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="Alarms:" FontWeight="SemiBold" />
        <TextBlock Grid.Column="1" Text=" " />
        <TextBlock Grid.Column="2" Text="{Binding AlarmCount}" />
        <TextBlock Grid.Column="3" Text="Last:" FontWeight="SemiBold" />
        <TextBlock Grid.Column="4" Text="{Binding LastAlarmMessage}" TextTrimming="CharacterEllipsis" />
      </Grid>
    </Border>
  </Grid>
</UserControl>